DESCRIPTION

Bug 2300: specify ep_engine initfile at startup

I've made the engine configs into proplists instead of strings so we
can specify arbitrary key-value pairs, with 'engine' being the
path to the engine.

Change-Id: Ifd7acc0e622250fdeed0412e05a7c5846cdd8834


COMMENTS

